[package]
name = "lmc"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = "0.5.1"
rocket_db_pools = { version = "0.2.0", features = ["sqlx_postgres"] }
sealed_trait = "0.1.1"
sqlx = { version = "0.8.3", features = ["postgres", "macros"] }
